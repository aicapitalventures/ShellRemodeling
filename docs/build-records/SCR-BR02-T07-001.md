# SCR-BR02-T07-001 — OpenAI Kill-Switch, Spend-Control & First-Generation Precheck v1.0

**Record ID:** SCR-BR02-T07-001
**Version:** 1.0
**Status:** T07 PASSED — HOSTED ATOMIC RESERVATION VERIFIED / T08 AUTHORIZATION PENDING
**Effective date:** 2026-08-13
**Branch:** `build/02-secure-remodel-studio`
**Dependencies:** SCR-BR02-001 v1.0, SCR-BR02-CLOUD-001 v1.0, SCR-BR02-TEST-001 v1.0, SCR-BR02-PROMPT-001 v1.0, SCR-BR02-DATA-001 v1.0, SCR-AIVIS-001 v0.2

## Scope

This is a static precheck only. No OpenAI API key was configured, requested,
printed, stored, or used. No OpenAI endpoint, `generate-concept`, Supabase Auth,
RLS, billing, or public website path was invoked or changed. No customer photo,
customer PII, or customer data was used.

## A. Kill Switch

**PASS.** `generate-concept` computes:

- `BR02_OPENAI_ENABLED` as enabled only when its value is exactly `true`;
- `BR02_KILL_SWITCH` as closed unless its value is exactly `false`.

Therefore an absent `BR02_OPENAI_ENABLED`, `BR02_OPENAI_ENABLED=false`, an
absent `BR02_KILL_SWITCH`, or `BR02_KILL_SWITCH=true` returns HTTP 503
`GENERATION_DISABLED` before source download or the OpenAI request function.
OpenAI can be reached only when both `BR02_OPENAI_ENABLED=true` and
`BR02_KILL_SWITCH=false` are deliberately configured.

## B. Missing OpenAI Secret

**PASS.** With the two switches deliberately open but `OPENAI_API_KEY` absent,
`generate-concept` returns HTTP 503 `GENERATION_DISABLED` before any provider
request. The key is read only in the server-side Edge Function and is never
returned in a response.

## C. Project Concept Cap

**PASS.** The adopted limit is four concepts per project.
The new `br02_reserve_generation` database function checks the project count and
the requested ordinal under a project advisory transaction lock, then creates
the `generating` concept and its attempt reservation in the same transaction.
The function is revoked from `public`, `anon`, and `authenticated` and granted
only to `service_role`; browser JavaScript cannot bypass it.

## D. User Rate Limit

**PASS.** The adopted server-side limit is eight attempt
events for an authenticated user in the preceding rolling 24 hours. The atomic
reservation locks by user ID before counting and inserting the new attempt.

## E. IP Rate Limit

**PASS.** The adopted server-side limit is three attempt
events for one hashed IP in the preceding rolling hour. `hmacIpHash` derives an
HMAC-SHA-256 value from the forwarded edge IP using `BR02_IP_HASH_SALT`; only
that derived value is stored in `generation_events`, never the raw IP.

The earlier source path could omit this quota when the salt or an edge IP header
was unavailable. It now fails closed with HTTP 503 `GENERATION_DISABLED` before
reservation, source download, or provider contact whenever an IP hash cannot be
derived.

## F. Spend Control

**PASS.** The application defaults remain a $20 monthly
ceiling and a $0.08 reserved cost for each attempted generation, sourced from
`BR02_MONTHLY_BUDGET_USD` and `BR02_RESERVED_COST_PER_CALL_USD` with those
fail-safe defaults. The reservation function serializes monthly checks with an
advisory transaction lock and writes the attempt reservation atomically, so
concurrent requests cannot pass the same available-budget calculation.

The previous read-then-insert implementation could overspend under concurrency.
The hosted migration is applied and behaviorally verified. Application controls
remain defense in depth and do not replace OpenAI Platform billing controls.

## G. OpenAI Project and Billing Isolation

**VERIFIED / T08 CONTROLS REMAIN.** The dedicated OpenAI project is
`Shell & Co Remodel Studio`, not the OpenAI Default project or a project shared
with another venture. A dedicated project-scoped BR02 API key is verified
restricted: Images (`/v1/images`) has Request permission and all other key
permissions are None. `OPENAI_API_KEY` is installed server-side in the dedicated
Supabase project and is not stored in Git.

The OpenAI Project Limits UI evidences a $5.00 project spend limit, current
displayed spend of $0.00 / $5.00, a 50% spend alert at $2.50, a 100% spend alert
at $5.00, and `gpt-image-2` as the only allowed model. The $5.00 prepaid balance
and auto-reload-off state remain separately evidenced; this UI evidence does not
re-verify auto-reload. These controls do not authorize provider access or T08.

## H. Timeout and Retry

**PASS.** `callOpenAI` establishes one 120-second total deadline shared by all
attempts. It permits an initial request plus at most two retries, with bounded
exponential backoff plus jitter, only for HTTP 429 and 5xx provider responses.
The retry delay must still fit within the original deadline. Abort errors return
`GENERATION_TIMEOUT`; non-retryable 4xx responses and network errors normalize
to `GENERATION_FAILED` without exposing provider details.

## I. Moderation and Error Handling

**PASS.** Provider `moderation_blocked` normalizes to `MODERATION_BLOCKED` and
sets the concept state to `blocked`. Invalid or non-ready source assets return
`UPLOAD_NOT_READY`; unsupported/non-retryable provider responses, malformed
success payloads, and provider failures normalize to `GENERATION_FAILED`; 429
and 5xx are bounded as described above; deadline expiry normalizes to
`GENERATION_TIMEOUT`. The JSON response allowlist contains only normalized error
codes, never an OpenAI key, Supabase secret, stack trace, or raw provider body.

## J. Model and Request Policy

**PASS.** The static request policy is `gpt-image-2` Image Edits, one image
(`n=1`) per call, `medium` quality, `1536x1024`, and WEBP output. The result is
stored in the private results bucket with a SHA-256 digest and provenance record.

## K. Prompt Governance

**PASS.** The server-side compiler preserves source-space truth, preserve/change/
must-have items, design direction, accessibility goals, and source-photo
geometry rules. Its explicit conceptual-visualization disclosure prohibits
construction or engineering drawings, permit/code approval, final dimensions,
guaranteed materials, and construction quotes. Only the compiler version and
prompt hash are persisted; raw compiled prompt text is not stored by default.

## Source Hardening Applied

The T07 audit found two material pre-generation flaws and corrected them without
opening the OpenAI gate:

1. Quota and budget checks were separate read-then-write operations and could be
   bypassed by concurrent requests. Migration
   `20260813000300_br02_atomic_generation_reservation.sql` creates the locked,
   service-only atomic reservation function; `generate-concept` now uses it.
2. A missing IP-hash salt or edge IP header could omit the IP rate limit.
   `generate-concept` now returns `GENERATION_DISABLED` when it cannot derive an
   IP HMAC.

## Static Validation

`git diff --check` passed. A local Node.js assertion check confirmed the exact
switch predicates, missing-secret return, IP-hash fail-closed return, 120-second
deadline, bounded retry predicate, WEBP request policy, atomic RPC invocation,
adopted cap/rate/budget values, advisory locks, and service-only SQL grants.
No local SQL parser, Supabase CLI, or Deno runtime was available during the
static review. Hosted migration application and behavior verification are
recorded below.

## Hosted Migration and RPC Verification

Migration `br02_atomic_generation_reservation` was applied to the dedicated
Shell & Co Remodel Studio Supabase project `mlxboidajkqyayxjdcvh`. The hosted
`public.br02_reserve_generation` function exists and is verified `SECURITY
DEFINER`.

EXECUTE privilege verification passed:

- `public`: denied;
- `anon`: denied;
- `authenticated`: denied;
- `service_role`: allowed.

Synthetic hosted behavioral verification passed with no raw IP used or persisted:

- valid reservation succeeded;
- wrong owner returned `NOT_FOUND`;
- non-ready source returned `UPLOAD_NOT_READY`;
- duplicate ordinal returned `RATE_LIMITED`;
- four concepts per project were allowed and the next reservation was denied;
- eight user attempts across synthetic projects were allowed and the ninth
   returned `RATE_LIMITED`;
- three attempts for one deterministic synthetic IP hash were allowed and the
   fourth returned `RATE_LIMITED`;
- a $0.08 reservation against a $0.01 ceiling returned
   `BUDGET_LIMIT_REACHED`;
- successful reservations created a `remodel_concepts` row with `generating`
   status and the corresponding `generation_events` `attempt` row.

The concept and event insertion was verified as one successful reservation pair.
The implementation contains transaction-scoped advisory locks for month, user,
IP hash, and project. Sequential hosted quota behavior passed. A separate
simultaneous multi-connection concurrency stress test has not been executed;
concurrent overspend/rate-limit bypass protection is classified as **HOSTED
IMPLEMENTATION VERIFIED / SEPARATE CONCURRENCY STRESS TEST NOT YET EXECUTED**.

All synthetic hosted fixtures were deleted after verification. Post-cleanup
counts were zero synthetic projects, assets, concepts, and generation events.

## Hosted Advisor Result

No new material performance error or warning was introduced by this migration.
The remaining performance INFO is `unused remodel_projects_retention_idx`.

No migration-specific critical security defect was identified. Existing advisor
findings remain unchanged: INFO RLS-enabled/no-client-policy notices for
service-side tables; WARN anonymous-access policy notices because anonymous Auth
is enabled; and WARN leaked-password protection disabled. These findings were not
changed during T07 and require separate controlled review.

## Hosted Function Alignment

The dedicated Shell & Co Remodel Studio project currently reports
`generate-concept` ACTIVE at hosted version 6. Connected Supabase control-plane
inspection returned the complete three-file source bundle:
`generate-concept/index.ts`, `_shared/core.ts`, and `_shared/prompt.ts`. The
source text matches the corresponding current files on the
`build/02-secure-remodel-studio` branch. The hosted `verify_jwt=true` setting
matches `[functions.generate-concept] verify_jwt = true` in the repository
configuration. The control-plane `ezbr_sha256` is recorded as hosted evidence
only; it is not claimed to equal Git blob SHAs, and the hosted bundle digest was
not independently reproduced locally.

Hosted verification passed:

- Supabase platform-level JWT verification remains enabled with `verify_jwt=true`;
- `requireUser(req)` remains separate application-level authentication logic;
- source contains the `BR02_OPENAI_ENABLED` fail-closed control;
- source contains the `BR02_KILL_SWITCH` fail-closed control;
- source fails closed when an IP hash is unavailable;
- source uses `br02_reserve_generation` before provider access;
- the provider path remains behind all fail-closed controls;
- no migration was reapplied;
- no Supabase secret was changed by this deployment;
- no generation was invoked and no OpenAI request was made;
- all other Edge Functions remain at their prior versions.

An intermediate hosted version 4 was created during deployment verification and
immediately superseded because its dependency bundle did not exactly match the
approved GitHub branch. Version 4 is not controlling and is not evidence for
T07. Hosted version 5 remains the previous governing aligned deployment and is
not invalidated by this reconciliation. Hosted version 6 supersedes version 5
only as the current active/control-plane deployment. The cause or actor
responsible for the version 5 to version 6 increment has not been reconstructed
in this record.

## T08 Prerequisites

Do not open T08 until all of the following are explicitly authorized and
evidenced:

1. `BR02_IP_HASH_SALT` is verified present by Supabase Custom secrets name-level UI
   evidence only; this does not yet verify forwarded-IP availability or HMAC
   runtime behavior, and the hosting edge must still confirm a usable forwarded
   IP header without persisting or exposing a raw IP.
2. Keep `BR02_OPENAI_ENABLED=false` and `BR02_KILL_SWITCH=true` until a separate
   explicit T08 authorization opens both for one test window.
3. Prepare one synthetic anonymous user, one synthetic bathroom image, one
   PII-free project, and exactly one `gpt-image-2` Image Edits request.
4. Verify private result storage, signed retrieval, prompt/version provenance,
   cost reservation/event evidence, and deletion/cleanup after that one result.

## Gate Decision

**T07 — PASS.** Static fail-closed review, source hardening, hosted atomic
reservation verification, dedicated OpenAI project isolation, restricted
project-scoped key, server-side key installation, and the evidenced prepaid/
auto-reload, spend-limit, and spend-alert controls are complete. IP-hash backend
secret/runtime confirmation, synthetic fixtures, and separate first-generation
authorization remain T08 gates. T08 is not authorized.

## OPENAI GATE

**CLOSED**

Until a separate founder-authorized first-generation gate is passed:

- `BR02_OPENAI_ENABLED` must remain false.
- `BR02_KILL_SWITCH` must remain true.
- `OPENAI_API_KEY` is installed server-side in Supabase; its raw value must never
   be exposed to browser/client code, Git, logs, or chat.
- installing `OPENAI_API_KEY` alone does not authorize or permit provider access.
- `generate-concept` must not be invoked for an OpenAI generation until separate
   T08 authorization.
- no customer photo or customer PII may be transmitted to OpenAI before that
   authorization.

T07 PASS does not itself authorize T08 or any OpenAI request.

## T08 ELIGIBILITY

**NOT AUTHORIZED**

## Pre-Generation Evidence Plan

This plan records safe prerequisite work only. It does not open either provider
gate, invoke `generate-concept`, call OpenAI, create customer data, or create a
synthetic fixture in the hosted project.

### Prerequisite Classification

- **OpenAI project usage/budget alerts — VERIFIED.** The OpenAI Project Limits UI
   evidences a $5.00 project spend limit, current displayed spend of $0.00 / $5.00,
   a 50% spend alert at $2.50, a 100% spend alert at $5.00, and `gpt-image-2` as
   the only allowed model. The $5.00 prepaid balance and auto-reload-off state
   remain separately evidenced; this UI evidence does not re-verify auto-reload.
- **`BR02_IP_HASH_SALT` hosted secret configuration — VERIFIED PRESENT.** Verified by
   Supabase Custom secrets name-level UI evidence only: the secret name
   `BR02_IP_HASH_SALT` is present in the hosted project's custom secrets list.
   No raw secret value was inspected, reproduced, logged, or exposed. This does
   not yet verify forwarded-IP availability or HMAC runtime behavior.
- **Forwarded-IP availability — REQUIRES SAFE HOSTED VERIFICATION / BLOCKING.**
   `generate-concept` returns `GENERATION_DISABLED` when no IP hash is derived.
   The hosting edge must safely confirm that an approved forwarded-IP header is
   available, without persisting or exposing a raw IP.
- **Synthetic anonymous bathroom fixture — PREPARABLE LOCALLY / HOSTED USE DEFERRED / BLOCKING FOR T08 EXECUTION.**
   The fixture must be prepared from non-customer, non-PII synthetic content and
   remain local or be uploaded only during an explicitly authorized T08 setup.

### Pre-T08 Work That May Proceed

1. Perform a hosted presence-only check for `BR02_IP_HASH_SALT`; never print,
    compare, export, or log its value.
2. Perform a safe hosted edge check for forwarded-IP availability using only a
    synthetic marker and a derived hash, never a raw IP persisted in records.
3. Prepare a synthetic bathroom fixture locally, with no customer photo, PII,
    or original filename metadata, but do not upload or use it for generation.

### Deferred T08 Lifecycle Evidence

The following cannot be completed by this checklist and must wait for a separate
explicit one-request T08 authorization: opening both gates for one test window,
the single synthetic generation, private result storage, signed result retrieval
and expiry, prompt/version/hash provenance, measured cost/event evidence, result
deletion, signed-URL invalidation, and final cleanup verification.

## First-Generation Authorization Gate

Opening actual OpenAI generation requires a separate explicit authorization after
the T08 prerequisites above are complete. That authorization must be limited to
one synthetic request and must not involve customer data.