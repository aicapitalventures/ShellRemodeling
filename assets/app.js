const qaStyles=document.createElement('link');qaStyles.rel='stylesheet';qaStyles.href='assets/qa-fixes.css';document.head.appendChild(qaStyles);

const state={photo:null,style:'Clean Modern',preserve:new Set(),change:new Set(['Tile','Vanity']),must:new Set(),selectedConcept:null};
const $=(s)=>document.querySelector(s);const $$=(s)=>[...document.querySelectorAll(s)];

function setPressed(btn,pressed){btn.setAttribute('aria-pressed',pressed?'true':'false')}
function chipGroup(selector,set){$$(selector).forEach(btn=>{setPressed(btn,btn.classList.contains('active'));btn.addEventListener('click',()=>{const v=btn.dataset.value;if(set.has(v)){set.delete(v);btn.classList.remove('active');setPressed(btn,false)}else{set.add(v);btn.classList.add('active');setPressed(btn,true)}updatePacket()})})}
chipGroup('[data-group="preserve"]',state.preserve);chipGroup('[data-group="change"]',state.change);chipGroup('[data-group="must"]',state.must);
$$('[data-style]').forEach(btn=>{setPressed(btn,btn.classList.contains('active'));btn.addEventListener('click',()=>{$$('[data-style]').forEach(x=>{x.classList.remove('active');setPressed(x,false)});btn.classList.add('active');setPressed(btn,true);state.style=btn.dataset.style;updatePacket()})});

const input=$('#spacePhoto'),drop=$('#dropzone'),preview=$('#photoPreview'),photoLabel=$('#photoLabel');
drop.tabIndex=0;drop.setAttribute('role','button');drop.setAttribute('aria-label','Choose or drop a bathroom or project photo');
drop.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();input.click()}});

function loadFile(file){if(!file)return;if(!/^image\/(jpeg|png|webp)$/.test(file.type)){alert('Please choose a JPG, PNG, or WEBP image.');return}if(file.size>10*1024*1024){alert('For this prototype, use an image under 10 MB.');return}const r=new FileReader();r.onload=e=>{state.photo={name:file.name,size:file.size,url:e.target.result};preview.src=e.target.result;preview.style.display='block';photoLabel.textContent=file.name;updatePacket()};r.readAsDataURL(file)}
input.addEventListener('change',e=>loadFile(e.target.files[0]));['dragenter','dragover'].forEach(evt=>drop.addEventListener(evt,e=>{e.preventDefault();drop.classList.add('drag')}));['dragleave','drop'].forEach(evt=>drop.addEventListener(evt,e=>{e.preventDefault();drop.classList.remove('drag')}));drop.addEventListener('drop',e=>loadFile(e.dataTransfer.files[0]));

function list(set){return [...set].length?[...set].join(', '):'None selected'}
function readValue(selector,fallback='Not selected'){const el=$(selector);return el&&el.value?el.value:fallback}
function updatePacket(){const el=$('#packetText');if(!el)return;el.replaceChildren();const rows=[['Source',state.photo?state.photo.name:'No customer photo yet'],['Project type',readValue('#roomType')],['Direction',state.style],['Preserve',list(state.preserve)],['Change',list(state.change)],['Must-have',list(state.must)],['Planning budget',readValue('#budget','Not sure yet')]];if(state.selectedConcept)rows.push(['Preferred concept',state.selectedConcept]);rows.forEach(([label,value],i)=>{const strong=document.createElement('strong');strong.textContent=label+': ';el.appendChild(strong);el.appendChild(document.createTextNode(value));if(i<rows.length-1)el.appendChild(document.createElement('br'))})}
['#roomType','#budget'].forEach(selector=>{const el=$(selector);if(el)el.addEventListener('change',updatePacket)});updatePacket();

$('#generateBtn').addEventListener('click',()=>{if(!state.photo){alert('Upload a bathroom or project photo first so the Remodel Studio can demonstrate the intended flow.');return}const room=$('#roomType').value;if(!room){alert('Choose the project type first.');return}$('#concepts').hidden=false;$('#generateBtn').textContent='Regenerate Prototype Concepts';$('#generationStatus').textContent='Prototype mode: four concept slots prepared. Production will call a secure OpenAI-backed service; no API key or customer photo will be stored in this public page.';$('#concepts').scrollIntoView({behavior:'smooth',block:'nearest'})});

const preferredConceptInput=document.querySelector('input[disabled][value="Choose above if desired"]');
$$('.concept-select').forEach(btn=>{setPressed(btn,false);btn.addEventListener('click',()=>{const card=btn.closest('.concept');$$('.concept').forEach(x=>x.classList.remove('selected'));card.classList.add('selected');state.selectedConcept=card.dataset.name;$$('.concept-select').forEach(x=>{x.textContent='Select';setPressed(x,false)});btn.textContent='Selected';setPressed(btn,true);if(preferredConceptInput)preferredConceptInput.value=state.selectedConcept;updatePacket();$('#reviewPanel').hidden=false})});

$('#estimateForm').addEventListener('submit',e=>{e.preventDefault();if(!e.currentTarget.reportValidity())return;const fd=new FormData(e.currentTarget);const packet={metadata:{record_type:'Shell & Co Pre-Estimate Intelligence Packet',prototype:true,created_at:new Date().toISOString(),response_expectation:'within 24 hours'},customer:{name:fd.get('name'),phone:fd.get('phone'),email:fd.get('email'),zip:fd.get('zip'),property_status:fd.get('propertyStatus'),marketing_consent:fd.get('marketingConsent')==='on'},project:{project_type:fd.get('projectType'),studio_project_type:$('#roomType').value,planning_budget:fd.get('budget'),style:state.style,preserve:[...state.preserve],change:[...state.change],must_have:[...state.must],notes:$('#vision').value,selected_concept:state.selectedConcept,timing:fd.get('timing')},source_photo:{filename:state.photo?.name||null,note:'Photo bytes are intentionally not included in the public prototype packet.'},appointment_policy:{initial_phone_photo_review:'no charge',onsite_reservation_fee_usd:49,credit_if_hired_within_days:30,cancellation_notice_hours:24,travel_surcharge:'$1.50 per additional one-way mile beyond 35 miles from Charlestown, Indiana'},disclosures:['AI visualization is conceptual, not a construction drawing or quote.','Planning budget is qualification context, not a price commitment.','Buildability, measurements, code, trade, permit, pricing and material availability require field verification.','Production photo transmission requires the adopted privacy, storage, retention and deletion controls.']};const blob=new Blob([JSON.stringify(packet,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const dl=$('#downloadPacket');if(dl.href)URL.revokeObjectURL(dl.href);dl.href=url;dl.download='shell-co-pre-estimate-prototype.json';dl.hidden=false;$('#formResult').style.display='block';$('#formResult').scrollIntoView({behavior:'smooth',block:'nearest'})});

$('#copyPhone').addEventListener('click',async()=>{try{await navigator.clipboard.writeText('502-303-2398');$('#copyPhone').textContent='Phone Copied'}catch{location.href='tel:+15023032398'}});
